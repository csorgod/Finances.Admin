@{
    string[] bread = (string[])ViewData["breadcrumb"];
}

<div class="block-header">
    <div class="row clearfix">
        <div class="col-md-6 col-sm-12">
            <h1>@bread[bread.Length - 1]</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="@Url.Action("Index","Home")">Início</a>
                    </li>
                    @foreach(string item in bread)
                    {
                        <li class="breadcrumb-item active" aria-current="page">
                            @item
                        </li>
                    }
                </ol>
            </nav>
        </div>            
        <!-- <div class="col-md-6 col-sm-12 text-right hidden-xs">
            <a href="javascript:void(0);" class="btn btn-sm btn-primary btn-round" title="">Create Campaign</a>
        </div> -->
    </div>
</div>